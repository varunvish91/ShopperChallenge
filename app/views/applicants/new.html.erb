<!DOCTYPE html>

<head>
  <title>VarunChallenge</title>
  <link href = "styles.css" rel = "stylesheet">
  <script type="text/javascript" src="http://code.jquery.com/jquery-latest.min.js"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.0.7/angular.min.js"></script>
  <script src="controller.js"></script>
</head>

<body>

	<div ng-app="CodingChallenge">
  <div ng-controller="CodingChallengeController">
<div class = "innerBox">

  <div class = "centered">
    <br>
    <img src = "logo.png">
  </div>

  <div class = "centered" ng-show = "show==1">
    <button class = "start apply" type="submit" value="submit" ng-click="begin()">Apply Now</button>
  </div>

  <div class = "signup centered" ng-show = "show==2">
      <%= form_for(@applicant) do |f| %>
        <br>
				<%= f.text_field :first_name, :placeholder => "First Name" %>
				<br>
				<%= f.text_field :last_name, :placeholder => "Last Name" %>
       	<br> 
				<%= f.text_field :phone, :placeholder => "Phone Number" %>
				<br>
				<%= f.collection_select :phone_type, Applicant::PHONE_TYPES, :to_s, :to_s, :include_blank => false %>
				<br>
				<%= f.text_field :workflow_state, :placeholder => "test" %>
				<br>
				<%= f.collection_select :region, Applicant::REGIONS, :to_s, :to_s, :include_blank => false %>
      	<br>
				<%= f.submit "Apply", class: "apply" %>
        <% end %>
        <br>
  </div>
</div>
</div>
</body>

</html>
